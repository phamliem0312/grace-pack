define('grace-pack:views/fields/body-images', ['views/fields/base'], function (Dep) {
    return Dep.extend({
        detailTemplate: 'grace-pack:fields/body-images',
        editTemplate: 'grace-pack:fields/body-images',
    })
});